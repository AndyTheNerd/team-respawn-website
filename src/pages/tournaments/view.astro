---
// Redirect /tournaments/view?id=trn_... → /tournaments/trn_...
// This preserves old shared links.
---
<script>
  const id = new URLSearchParams(window.location.search).get('id');
  if (id) {
    window.location.replace(`/tournaments/tournament?id=${encodeURIComponent(id)}`);
  } else {
    window.location.replace('/tournaments');
  }
</script>
