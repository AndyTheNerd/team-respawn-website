---
export function getStaticPaths() {
  // Generate redirects for old paginated URLs (e.g., /blog/1, /blog/2, etc.)
  return [1, 2, 3, 4, 5].map((page) => ({
    params: { page: String(page) },
  }));
}

// All pagination is now handled client-side on /blog
// Redirect any old paginated URLs to the main blog page
return Astro.redirect('/blog', 301);
---
